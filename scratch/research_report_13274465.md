"## Complete Electron Runtime Infrastructure Research Report\n\nI've thoroughly investigated all requested files. Here is the complete report with exact paths, line numbers, function names, and code snippets.\n\n---\n\n## 1. Electron Acquisition Script\n\n**File**: `/home/charleton/Desktop/agentProjects/Codex/macRun/runtime/third_party/electron/acquire.sh` (284 lines)\n\n### Full Logic Summary:\n\n**Pinned Versions** (L22-28):\n- `22.3.27` (SHA256: `631d8eb...`)\n- `24.8.8` (SHA256: `f4604c0...`)\n- `28.3.3` (SHA256: `20f6be4...`)\n\n**Cache location**: `~/.cache/macrun/electron/` (L15)\n\n**CLI options** (L34-43): `--version X.Y.Z`, `--all`, `--dry-run`, `--verify-only`, `--cache-dir`\n\n**Core function `acquire_version()`** (L57-143):\n1. Checks if `$CACHE_DIR/electron-$VERSION/electron` already exists (L64-67)\n2. Downloads from `https://github.com/electron/electron/releases/download/v${VERSION}/electron-v${VERSION}-linux-x64.zip` (L61)\n3. SHA256 verification against hardcoded hashes (L86-100)\n4. Extracts with `unzip` to `$CACHE_DIR/electron-$VERSION/` (L104-109)\n5. `chmod +x` the binary (L115)\n6. Writes per-version `manifest.json` with integrity info (L124-140)\n\n**Verification function `verify_cached()`** (L148-197):\n- Reads `binary_hash` from each version's `manifest.json`\n- Recomputes SHA256 and compares\n\n**Manifest generation** (L237-275): After acquisition, writes `electron-versions.json` to `compat-db/manifests/electron/` with the full pinned version mapping including `discovery_contract` with `\"lookup_key\": \"CFBundleIdentifier\"`, `\"version_detection\": \"Electron Framework.framework version string\"`, `\"fallback\": \"latest-compatible-cached\"`.\n\n**Shim installation** (L202-209): Calls `runtime/shims/install.sh` if it exists.\n\n---\n\n## 2. Electron Adapter\n\n**File**: `/home/charleton/Desktop/agentProjects/Codex/macRun/platform/adapters/electron_adapter.cpp` (1118 lines)\n**Header**: `/home/charleton/Desktop/agentProjects/Codex/macRun/platform/adapters/electron_adapter.hpp` (148 lin
<truncated 16783 bytes>